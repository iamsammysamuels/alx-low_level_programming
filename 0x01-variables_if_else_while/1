#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i, j;
for (i = 0; i < 9; i++)
{
for (j = i + 1; j < 10; j++)
{
putchar(% 10, i);
putchar(% 10, j);
putchar(',');
putchar(' ');
}
}
putchar('\n');
return (0);
}
