#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers and returns the result
 *@x: print int x
 *@y: print int y
 * Return: Always 0 (Success)
 */
int add(int x, int y)
{
return (x + y);
}
